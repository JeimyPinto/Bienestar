import path from 'path';

export const config = path.resolve('config/config.json');
export const modelsPath = path.resolve('models');
export const seedersPath = path.resolve('seeders');
export const migrationsPath = path.resolve('migrations');
